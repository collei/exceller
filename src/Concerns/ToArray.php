<?php
namespace Collei\Exceller\Concerns;

/**
 * Allows capture all data at once.
 */
interface ToArray
{
	/**
	 * Allows capture all data at once.
	 *
	 * @return void
	 */
	public function array(array $array);
}
